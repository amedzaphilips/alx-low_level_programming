#include <stdio.h>
/**
 * main - a program that prints a statement with puts
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
